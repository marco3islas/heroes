import { NodescripcionPipe } from './nodescripcion.pipe';

describe('NodescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new NodescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
